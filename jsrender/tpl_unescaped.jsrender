<html>
	<head>
		<title>{{: title}}</title>
	</head>
	<body>
		<p>{{: text}}</p>
		{{if projects.length }}
			{{for projects }}
				<a href="{{: url}}">{{: name }}</a>
				<p>{{: description }}</p>
			{{/for}}
		{{else}}
			No projects
		{{/if}}
	</body>
</html>
