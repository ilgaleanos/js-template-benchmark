<html>
	<head>
		<title>{{ title }}</title>
	</head>
	<body>
		<p>{{ text }}</p>
		{{# projects.length }}
			{{# projects }}
				<a href="{{url}}">{{ name }}</a>
				<p>{{ description }}</p>
			{{/ projects }}
		{{/ projects.length }}
		{{^ projects.length }}
			No projects
		{{/ projects.length }}
	</body>
</html>
